# Astra 通用实施底座（不能替代具体子任务）

实施我提供的那个子任务，而不是一次实现整个Mxx。先实际读取AGENTS、CONTRACTS、PROJECT_STATE、当前子任务、直接前置handoff及相关代码；读取当前分支/状态/锁，并把实际commit写入报告。旧PDF/启动Prompt与当前ADR冲突先说明，不另起接口。

只改授权路径。已有成果先测试/核对再差量补齐；不覆盖用户未提交改动，不reset/clean，不替换infra。共享契约、锁和迁移只有指定集成人可改；没有独立工作树就串行。

先定义关键断言，再用当前锁定工具实现与测试。默认离线，live/云机/破坏性测试必须已有具体授权和总预算。工具无权限、前置不成立或关键试验失败时带证据停止；不靠假返回、删难例、关测试、放宽安全校验伪装成功。

输出修改文件/原因、真实命令/退出码/结果、未执行项、版本/资源变化、回滚方式、下游输入和handoff。提交只包含自己的允许路径；需推送时检查远端分支与基线，禁止force。没有本机/提交权限就明确交付停在哪一步。
