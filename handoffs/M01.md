# M01 交接

状态：`IMPLEMENTED_OFFLINE_VERIFIED`。实施/集成日期：2026-09-13；环境：Windows PowerShell、Python 3.12.14、uv 0.12.13。实施前工作区干净，分支 `main`、HEAD `e8955e4b357a229c53c69592042273b69831a96a`；本轮未自动 commit/push。

## 已实现

在 `modules/deephelp-app/` 创建首个 Python 业务包，保留根 workspace、单一根 `uv.lock`、Python 3.12 和既有 `infra/`。新增 FastAPI、Pydantic、HTTPX、Uvicorn 与 pytest/pytest-asyncio/Ruff/mypy；没有安装 LangGraph、AgentScope、GPU、前端或任务队列依赖。

| M01 要求 | 实现及验证证据 |
|---|---|
| 统一客户端启动/关闭 | `app.py` lifespan + AsyncExitStack；启动中途失败和真实 loopback Uvicorn 正常退出均关闭资源 |
| health / 有类型占位 converse | `/health` HTTP 200；`/converse` HTTP 501、NOT_IMPLEMENTED；无模型/业务调用、无 run/question、事实/证据为空 |
| 唯一最小 DTO 与身份 | `domain/models.py`；入口独立本地测试身份、正文拒绝自报身份；JSON round-trip、未知版本 null 与跨归属 fake 读隔离测试 |
| 错误封装 / 请求诊断 ID | 422/401/500/504 类型化响应；安全静态错误消息、UUID 格式头校验、响应头/正文一致；失败仍报告已消耗预算 |
| JSONL trace / 两请求隔离 | `trace.py`；交错请求 ContextVar 校验；JSONL 可解析、不包含正文/凭据；短文件写在线程中串行完成 |
| fake ModelGateway / Repository | `ports.py` Protocol + `fakes.py`；固定合成输出、并发峰值约束、tenant/user/channel/message 范围、深复制 |
| 总 deadline / 子 timeout / 共享预算 | `execution.py`；排队包含在总 deadline、重试不重建预算、精确调用/重试数、非只读不重试、编程异常与取消传播 |
| 取消释放连接和任务 | 真实 httpcore 连接池 + 本机合成 HTTP 服务；取消应用请求后对端观测 EOF，max_connections=1 的下一次读取成功，所有任务收尾 |
| 缺密钥 fake 可用 / live 明确拒绝 | fake 无密钥启动并默认拒绝网络；live 缺密钥在启动/pytest 配置阶段报字段名；完整配置仍报告真实 Gateway NOT_IMPLEMENTED |
| 三个异步实验与 Java 类比 | `experiments.py`、`docs/M01_ASYNC_GUIDE.md`；独立 I/O 峰值、TaskGroup 失败/超时收尾、CPU 阻塞与进程 heartbeat 对照 |
| 分层测试 / 最小 CI | unit/integration/live/e2e 目录；根 `conftest.py` 提前注册 live 参数并限制预算；默认阻止外部 DNS/连接；GitHub Actions locked sync、静态检查与离线 pytest |

依赖实锁版本：FastAPI 0.141.1、Pydantic 2.13.5、HTTPX 0.28.1、Uvicorn 0.52.4、pytest 8.4.2、pytest-asyncio 0.26.0、Ruff 0.16.7、mypy 1.20.2。全部安装在既有根 `.venv` 中，业务解释器不是 uv 启动器使用的 Python 3.11。

## 实际执行与退出码

以下命令均从仓库根执行；`uv` 不在 PATH，遵循 M01 规格使用既有 `py -3.11 -m uv`。

| 命令 | 退出码 | 实际结果 |
|---|---|---|
| `py -3.11 -m uv --version` | 0 | uv 0.12.13 |
| `py -3.11 -m uv lock` | 0 | 根锁解析 30 个包；新增 workspace 成员及最小依赖 |
| `py -3.11 -m uv sync --locked --all-packages` | 0 | 成员构建成功，根环境同步完成 |
| `py -3.11 -m uv run --locked python --version` | 0 | Python 3.12.14 |
| `py -3.11 -m uv run --locked ruff check conftest.py modules/deephelp-app` | 0 | All checks passed |
| `py -3.11 -m uv run --locked ruff format --check conftest.py modules/deephelp-app` | 0 | 24 个 Python 文件格式通过 |
| `py -3.11 -m uv run --locked mypy` | 0 | 11 个源文件 strict 检查通过 |
| `py -3.11 -m uv run --locked pytest` | 0 | 49 项：48 passed、1 skipped；最近一次 6.26 秒；unit 33、integration 13、离线应用 e2e 2 通过，live 1 默认跳过 |
| `py -3.11 -m uv run --locked python -m deephelp_app.experiments` | 0 | 串行 0.2166 秒、并发 0.1098 秒，峰值 1/2；超时/失败均取消并收尾；CPU heartbeat 同步 0、进程 29 |
| `py -3.11 -m uv run --locked python .local/m01-check-local.py` | 0 | 所有 Git 可见文件不含本机密钥；子进程 pytest live 缺密钥期望退出 4、明确 MODEL_API_KEY 诊断 |
| `git check-ignore .env.local .local/qianwen-quota-2026-09-12.md` | 0 | 两个本机信息文件均被 Git 忽略 |
| `git diff --check` | 0 | tracked 差异无空白错误 |
| `rg --files -g uv.lock` | 0 | 仅根 `uv.lock` |

开发中曾出现 Ruff 行长/导入问题，已格式化/修正，最终检查退出 0。本机补查脚本第一次退出 1：pytest 从根启动时深层 conftest 的 live 参数未能提前注册；将门禁移至根 `conftest.py`，新增四个子进程 CLI 回归测试后通过。合成完整配置的 CLI 测试只执行配置校验，不访问真实模型。

耗时数值为本机一次观测，不是毫秒级 CI 门禁。确定性判据包括 I/O 并发峰值、取消事件顺序、任务清理、有限重试计数与 heartbeat 是否继续。

## 未执行与限制

- 未重新连接/修改 P00 云环境；MySQL/Redis/Milvus 仍使用 P00 历史证据，不声称本轮真实集成通过。
- 未调用任何真实文本/embedding 模型、未消费免费额度、未重新查询额度/到期日。用户提供的 API 配置和 2026-09-12 额度快照仅存本机 Git 忽略文件；凭据/内部地址不进入可提交文档。
- GitHub Actions 配置已创建，本轮未在 GitHub 执行。README 的长期 CLI 服务启动命令未单独常驻运行；pytest 已实际启动 loopback Uvicorn、发送 HTTP 请求并验证正常关闭。
- offline e2e 验证应用组装和本机服务生命周期，不是业务 Agent、云服务或线上效果验收。取消用例验证应用任务取消向下传播，不声明公网断线自动取消、进程崩溃或审批恢复能力。
- FakeRepository 不是持久业务账本；M01 占位 API 不接受/记账真实业务消息，也未发布完整幂等/审批契约。live 仅有配置门禁，应用有完整配置仍拒绝真实调用，运行时 token/费用约束由 M03 补齐。
- 本轮未做 PDF 来源复核或新增架构裁决；直接依据已验收 M00 语义和 M01 规格实施，没有复制其它模块规格。

## 受影响路径与给 M02

可提交改动：根 `pyproject.toml`、`uv.lock`、`conftest.py`、`.env.example`、`.github/workflows/ci.yml`、`README.md`、`modules/README.md`、`modules/deephelp-app/`、`docs/M01_ASYNC_GUIDE.md`、`docs/PROJECT_STATE.md`、本 handoff。既有 `infra/`、M00 契约/ADR 与其它模块规格未修改。

本机忽略文件：`.env.local`、`.local/qianwen-quota-2026-09-12.md`、`.local/m01-check-local.py`；环境/缓存位于既有 `.venv` 和工具缓存，日志默认位于被忽略的 `logs/`。

M02 在 `deephelp_app.domain.models` 原位补全 Request/Response、Evidence、版本/状态/错误与存储语义，替换 M01 草案占位类型，不复制另一套 DTO。保持领域对象可序列化，客户端/锁/预算时钟在运行时资源中；继续使用相同共享客户端与 fake Ports。不得把 M01 `NOT_IMPLEMENTED` 响应当作业务执行完成。M03 后续再验证用户授权范围内的具体模型、实时免费额度、embedding signature 与单次调用预算。

本轮同一主写者负责实现、离线验证及模块集成，据以上证据更新 `docs/PROJECT_STATE.md`。
