# M00 交接

状态：**DESIGN_ACCEPTED**。2026-09-13执行S06并覆盖S01–S05，只表示M00设计验收；M01–M21仍未实现，本会话未开始M01。

本轮输入分支main，输入commit：`ebf8f46bf85f893305afa4318039341f6f199c0d`，初始工作树干净。架构版本M00-design-v1.2，唯一语义契约0.1.2-draft。实际验收commit：`1ae0a35c0c8f4a26d5a42b5d63185ca09292cdbe`；本段在该提交创建并成功推送后补录。

## 已核验与补齐

- S01：本地原PDF SHA-256/78页、p19/52/57/58/60/62–69原图，ZIP与42份解压稿逐字节哈希；MySQL/5GiB实验/根workspace与空业务锁。原件不入Git，历史P00未重测。
- S02/S03：ID、幂等、状态、错误、证据和共享预算；补3份JSON语义样例与空值区别；ADR018明确审批CAS、唯一执行领取、领取自身版本推进和重复决定返回语义。
- S04：更正、双问题交错、审批成功后崩溃/对账/重复批准，均补逐事件ID/版本/状态/计数/证据及负面分支，见[验收矩阵](../docs/MODULES/M00_ACCEPTANCE.md)。是设计fixtures，不是模拟业务引擎的PASS输出。
- S05：文档工具拒绝中间目录链接/Windows junction，将M00报告引用纳入自检；补实际junction、导入入口冲突、已跟踪私有目标、导出symlink策略测试。
- S06：完成**单会话双视角**对照，没有其他写入Agent，没有声称独立会话审查。

完整要求—产物—检查—结果—缺口表及失败记录见[S06报告](../reports/M00/S06.md)，逐命令证据见[validation-s06.json](../reports/M00/validation-s06.json)。首次交付与Windows归位历史分别保留在validation.json、validation-v2.json、windows-sync-v2.json。

## 实际检查

项目解释器`D:\IdeaProject\deephelp\.venv\Scripts\python.exe`，Python 3.12.14。实际执行`python scripts/project_context.py verify`、`python -B -m unittest discover -s tests/docs -v`（均附`-X utf8`），退出码均0；文档测试29项，28通过、1环境跳过。import-source返回ALREADY_PRESENT；export --module M00只输出到.local；git diff --check通过。

真实symlink创建测试受当前Windows权限限制，跳过不计通过；3项真实junction检查和1项模拟symlink策略检查已通过，后者不冒充OS集成。两个新增反例在旧工具上确实失败，修复后通过。中间测试清理顺序和fixture引用缺失的失败及修复已如实记录。

新增业务依赖/业务包/迁移/服务/模型调用/云变更均0。infra、根pyproject、uv.lock、modules、AGENTS未修改。没有业务unit/integration/live/e2e、当前云机health/容量、持久saver或真实崩溃恢复运行证据。

## 后续输入与门禁

给M01：保留根uv workspace与单锁，在modules/deephelp-app创建唯一首包，按domain/application/adapters布局实现离线异步骨架；占位必须明确未实现。此处仅交接，不执行M01。

给M02：导入同一套CONTRACTS 0.1.2-draft语义，原位补全M01最小类型；把S06 JSON和M00_ACCEPTANCE逐事件表转为冻结合成类型/兼容性测试，不复制DTO或在本轮建表。

未定运行门禁：M03真实模型授权、费用上限和embedding signature；M05真实集合/容量；M15 MySQL持久saver、下游幂等/查询、审批并发和崩溃重放。未验证之前不能宣称对应运行能力。这些不阻塞已完成的M00设计验收。

## 提交与远端证据

验收commit `1ae0a35c0c8f4a26d5a42b5d63185ca09292cdbe`，标题`docs(M00): accept S06 design and close evidence and path safety gaps`，仅涉及本次11个M00路径。

实际`git push origin HEAD:refs/heads/main`退出0；随后`git ls-remote origin refs/heads/main`退出0，远端main等于上述完整SHA；`git status --short`为空。未force或推送其它分支。后续记录提交只补真实SHA/推送证据，不改变已经检查的设计和工具；最终HEAD由会话末尾再次核对远端。
