# M00 交接

状态：`DESIGN_ACCEPTED`。这只表示架构设计通过，不表示业务 Agent、真实模型或恢复能力已经实现。

## 长期产物

- `docs/ARCHITECTURE.md`：13 段主流程、目录边界、数据所有权与故障责任
- `docs/CONTRACTS.md`：ID、状态、错误、证据、预算、审批与幂等语义
- `docs/DECISIONS.md`、`docs/RISKS.md`：原文与现仓库冲突的裁决和运行门禁
- `docs/ACCEPTANCE.md`：订单更正、双问题交错、审批后重放及未来分层验收
- `docs/SOURCE_MAP.md`：PDF 页码和可引用边界

M00 验收提交为 `1ae0a35c0c8f4a26d5a42b5d63185ca09292cdbe`。当时完成的是设计走查和文档工具验证；未运行业务 unit/integration/live/e2e，也未重新验收云环境。

## 给 M01

保留根 uv workspace 与单锁，在 `modules/deephelp-app/` 创建首个业务包。交付统一客户端生命周期、health/占位 converse、trace、fake gateway/repository 和可验证的异步练习；占位能力明确返回 `NOT_IMPLEMENTED`。不重建 P00，不提前安装 M13/M21 依赖。

## 给 M02

在 M01 的最小类型上原位补全 `docs/CONTRACTS.md` 规定的唯一 DTO，不另起第二套 Request/Response。把 `docs/ACCEPTANCE.md` 的合成场景转成类型与兼容性测试；此阶段不冒充真实数据库、审批或恢复验证。

仍待后续模块验证：M03 模型/费用，M05 真实检索容量，M15 持久 saver、审批并发和副作用恢复。
