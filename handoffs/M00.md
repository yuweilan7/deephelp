# M00 交接

状态：DESIGN_READY / REVIEW_PENDING。本轮是架构与规划文件交付，不是Agent业务实现。

输入commit：15d9ac9cd03251f7c32242cbdf1279abbffb8df9；文档版本M00-design-v1；语义契约0.1-draft。提交后用 `git log -1 --format=%H -- docs/MODULES/M00.md` 查询文档commit，不自引用未知SHA。

已交付：短AGENTS入口、源哈希/页码图索引、当前仓库差异ADR、13段架构、语义契约、风险、22份按需修订模块、M00详细设计/六个独立Prompt、验收矩阵、文档自检/上下文导出/本地源文件导入工具。

本轮实际检查见reports/M00/validation.json；测试对象是文档工具与目录一致性。原PDF未入Git，infra/pyproject/uv.lock未修改。新增常驻服务0，付费模型调用0，运行中数据库变更0。

未执行：Windows同步/原PDF复制、当前云机health、业务unit/integration/live/e2e、真实崩溃恢复、两个独立会话的契约对照。源图读取不等于作者源码审计；P00报告是历史证据。

下游可用：CONTRACTS的ID/状态/错误/预算/证据/版本语义；ARCHITECTURE的目录职责/13段/一致性责任；M01增量workspace布局；M02具体类型输入；直接依赖见planning/manifest.json。

未定：真实模型/费用/embedding_signature由M03验证；真实集合和容量M05验证；MySQL持久checkpoint后端M15验证。这些不阻塞M00设计或M01离线骨架，但阻塞相应运行能力的完成声明。

退出：S06独立复核通过，记录具体命令/结果/commit并由集成会话将PROJECT_STATE改为DESIGN_ACCEPTED。下一模块M01，不提前实施M02及之后。已有文档以差量修正为主，禁止重生成覆盖用户后续修改。

## v2补充：本地资料和规划复核

输入commit为4724a386ceecf62efe318569526103e3233d5db6。Windows已同步、PDF及原ZIP/解压稿已在.local/references归位，见windows-sync-v2.json；旧“Windows未执行”仅描述首次交付。当前M00设计版本v1.1、语义草案0.1.1-draft。

22份M规范保留核心目标/失败案例，新增分期边界；固定拆分改为四种自适应决策。一个M默认同一主会话顺序实施。S06按S01–S05覆盖表复核已有成果，不以文件存在代替证据，也不要求重做五个会话。此次未自动把M00设为DESIGN_ACCEPTED，不把Prompt审查当独立业务运行验收。

本轮真实命令/解释器/结果见reports/M00/validation-v2.json。没有业务实现、真实模型调用、当前云机复测或持久审批故障演练。下一步按修订后的S06做最终设计复核，通过后进入M01。
